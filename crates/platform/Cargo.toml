[package]
name = "platform"
description = "Portable trait surface and types for rustils — the api layer. No I/O, no unsafe."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
thiserror.workspace = true

[lints]
workspace = true
